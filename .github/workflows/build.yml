name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

# Read-only by default. Nothing here needs to write to the repository, and pull
# requests from forks must never get a writable token.
permissions:
  contents: read

env:
  SOLUTION: src/Shell.sln
  CONFIGURATION: release

jobs:
  build:
    name: ${{ matrix.platform }}
    runs-on: windows-latest

    strategy:
      fail-fast: false
      matrix:
        platform: [x64, x86, arm64]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Restore packages
        shell: pwsh
        run: |
          # VC-LTL still uses packages.config, so it needs nuget restore.
          # The WiX 5 installer project is SDK-style and needs an MSBuild restore.
          nuget restore ${{ env.SOLUTION }}
          msbuild -t:Restore ${{ env.SOLUTION }} `
            -p:Configuration=${{ env.CONFIGURATION }} `
            -p:Platform=${{ matrix.platform }}

      - name: Build
        shell: pwsh
        run: |
          msbuild -m ${{ env.SOLUTION }} `
            -p:Configuration=${{ env.CONFIGURATION }} `
            -p:Platform=${{ matrix.platform }}

      # The x64 build runs the suite automatically as a post-build step. The
      # cross-compiled builds cannot run on the host, so invoke it explicitly
      # here for x64 only and let a non-zero exit fail the job.
      - name: Test
        if: matrix.platform == 'x64'
        shell: pwsh
        run: |
          & "src/bin/tests.exe"
          if ($LASTEXITCODE -ne 0) { throw "tests failed with exit code $LASTEXITCODE" }

      - name: Checksums
        shell: pwsh
        run: |
          $files = Get-ChildItem src/bin -File |
                   Where-Object { $_.Extension -in '.msi', '.dll', '.exe' -and $_.Name -ne 'tests.exe' }
          $files | Get-FileHash -Algorithm SHA256 |
            ForEach-Object { "{0}  {1}" -f $_.Hash.ToLower(), (Split-Path $_.Path -Leaf) } |
            Tee-Object -FilePath src/bin/SHA256SUMS.txt

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: shell-${{ matrix.platform }}
          path: |
            src/bin/**
            !src/bin/*.lib
            !src/bin/*.exp
            !src/bin/*.ilk
            !src/bin/*.pdb
            !src/bin/*.wixpdb
            !src/bin/ca.dll
            !src/bin/tests.exe
          if-no-files-found: error

      # Kept separate so they are available for crash triage without being
      # mistaken for something to ship.
      - name: Upload symbols
        uses: actions/upload-artifact@v4
        with:
          name: symbols-${{ matrix.platform }}
          path: |
            src/bin/*.pdb
            !src/bin/tests.pdb
          if-no-files-found: error
