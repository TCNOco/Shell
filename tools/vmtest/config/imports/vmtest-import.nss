// Imported by ../shell.nss with a relative path. If this file is not found the
// sentinel below is undefined and the smoke test's import assertion fails,
// which is exactly the regression we want to catch.

$vmtest_imported_title = 'VMTEST_IMPORT_OK'
